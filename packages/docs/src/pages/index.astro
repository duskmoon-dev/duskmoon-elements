---
import BaseLayout from '../layouts/BaseLayout.astro';
import ThemeToggle from '../components/ThemeToggle.astro';
---

<BaseLayout
  title="Home"
  description="DuskMoon Elements - A web components library bridging @duskmoon-dev/core design to real projects"
>
  <div class="home-container">
    <header class="home-header">
      <div class="header-content">
        <span class="logo">DuskMoon Elements</span>
        <div class="header-actions">
          <ThemeToggle />
          <a
            href="https://github.com/duskmoon-dev/duskmoon-elements"
            class="github-link"
            target="_blank"
            rel="noopener noreferrer"
          >
            GitHub
          </a>
        </div>
      </div>
    </header>

    <main class="home-main">
      <div class="hero">
        <h1 class="hero-title">DuskMoon Elements</h1>
        <p class="hero-subtitle">
          A web components library that bridges the
          <code>@duskmoon-dev/core</code> design system to real-world projects.
        </p>
        <div class="hero-actions">
          <a href="/duskmoon-elements/docs/getting-started" class="btn btn-primary">
            Get Started
          </a>
          <a href="/duskmoon-elements/docs/components" class="btn btn-secondary">
            View Components
          </a>
        </div>
      </div>

      <section class="features">
        <div class="feature">
          <h3>Framework Agnostic</h3>
          <p>Built with native Web Components APIs. Works with React, Vue, Angular, or vanilla JavaScript.</p>
        </div>
        <div class="feature">
          <h3>Design System Ready</h3>
          <p>Seamlessly integrates with @duskmoon-dev/core CSS design tokens for consistent styling.</p>
        </div>
        <div class="feature">
          <h3>Tree Shakable</h3>
          <p>Import only the components you need. Each element is an independent package.</p>
        </div>
        <div class="feature">
          <h3>TypeScript First</h3>
          <p>Full TypeScript support with type declarations for excellent developer experience.</p>
        </div>
      </section>

      <section class="install">
        <h2>Quick Install</h2>
        <pre><code>npm install @duskmoon-dev/elements</code></pre>
        <p>Or install individual components:</p>
        <pre><code>npm install @duskmoon-dev/el-button @duskmoon-dev/el-card</code></pre>
      </section>
    </main>

    <footer class="home-footer">
      <p>&copy; 2025 DuskMoon. MIT License.</p>
    </footer>
  </div>
</BaseLayout>

<style>
  .home-container {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  .home-header {
    border-bottom: 1px solid var(--color-outline, #e0e0e0);
  }

  .header-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 1.5rem;
  }

  .logo {
    font-weight: 600;
    font-size: 1.25rem;
    color: var(--color-on-surface);
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .github-link {
    color: var(--color-on-surface);
    text-decoration: none;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    transition: background-color 0.2s;
  }

  .github-link:hover {
    background-color: var(--color-surface-container, rgba(0, 0, 0, 0.1));
  }

  .home-main {
    flex: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
    width: 100%;
  }

  .hero {
    text-align: center;
    padding: 4rem 0;
  }

  .hero-title {
    font-size: 3rem;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary, #8b5cf6));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-on-surface);
    opacity: 0.8;
    max-width: 600px;
    margin: 0 auto 2rem;
  }

  .hero-subtitle code {
    background-color: var(--color-surface-container, #f0f0f0);
    padding: 0.125rem 0.5rem;
    border-radius: 0.25rem;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 500;
    text-decoration: none;
    transition: transform 0.2s, box-shadow 0.2s;
  }

  .btn:hover {
    transform: translateY(-2px);
    text-decoration: none;
  }

  .btn-primary {
    background-color: var(--color-primary);
    color: var(--color-on-primary, white);
  }

  .btn-secondary {
    background-color: var(--color-surface-container, #f0f0f0);
    color: var(--color-on-surface);
    border: 1px solid var(--color-outline, #e0e0e0);
  }

  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    padding: 3rem 0;
  }

  .feature {
    padding: 1.5rem;
    border-radius: 1rem;
    background-color: var(--color-surface-container, #f5f5f5);
  }

  .feature h3 {
    margin-top: 0;
    color: var(--color-primary);
  }

  .feature p {
    margin-bottom: 0;
    opacity: 0.8;
  }

  .install {
    padding: 3rem 0;
    text-align: center;
  }

  .install h2 {
    margin-bottom: 1.5rem;
  }

  .install pre {
    display: inline-block;
    text-align: left;
    max-width: 100%;
    background-color: var(--color-surface-container-high, #1e1e1e);
    padding: 1rem 1.5rem;
    border-radius: 0.5rem;
    margin: 0.5rem 0;
  }

  .install pre code {
    color: var(--color-on-surface);
    background: none;
    padding: 0;
  }

  .install p {
    margin-top: 1.5rem;
  }

  .home-footer {
    text-align: center;
    padding: 2rem;
    border-top: 1px solid var(--color-outline, #e0e0e0);
    opacity: 0.7;
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }
  }
</style>
