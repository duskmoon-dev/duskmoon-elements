/**
 * Auto-register el-dm-tabs custom elements
 *
 * @example
 * ```ts
 * import '@duskmoon-dev/el-tabs/register';
 *
 * // Now you can use <el-dm-tabs>, <el-dm-tab>, and <el-dm-tab-panel> in HTML
 * ```
 */
import { register } from './index.js';

register();
