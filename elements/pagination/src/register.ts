/**
 * Auto-register el-dm-pagination custom element
 *
 * @example
 * ```ts
 * import '@duskmoon-dev/el-pagination/register';
 *
 * // Now you can use <el-dm-pagination> in HTML
 * ```
 */
import { register } from './index.js';

register();
