/**
 * Auto-register el-dm-switch custom element
 *
 * @example
 * ```ts
 * import '@duskmoon-dev/el-switch/register';
 *
 * // Now you can use <el-dm-switch> in HTML
 * ```
 */
import { register } from './index.js';

register();
