/**
 * Auto-registration entry point for el-dm-select
 *
 * Import this module to automatically register the custom element:
 * import '@duskmoon-dev/el-select/register';
 */

import { register } from './el-dm-select.js';

register();
